<form method="post" action="{{ path('productos_delete', {'id': producto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <a href="{{ path('productos_index') }}" class="btn btn-primary btn-success"> Regresar</a>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn btn-primary btn-danger">Delete</button>
</form>
